<!DOCTYPE html>
<html>
<head>
  <title>💖 Valentine 💖</title>
  <style>
    body {
      background-color: #ffd1dc;
      font-family: "Comic Sans MS", cursive, sans-serif;
      text-align: center;
      margin-top: 50px;
      overflow: hidden;
    }
    button, input {
      font-size: 20px;
      padding: 10px 20px;
      margin: 10px;
      cursor: pointer;
    }
    #no {
      position: absolute;
    }
    .heart {
      position: absolute;
      color: red;
      font-size: 24px;
      animation: float 4s linear infinite;
    }
    @keyframes float {
      0% {transform: translateY(0);}
      100% {transform: translateY(-600px);}
    }
  </style>
</head>
<body>

<!-- PASSWORD SCREEN -->
<div id="lock">
  <h1>This page is only for you 💕</h1>
  <p>Enter the secret code 😌</p>
  <input type="password" id="pass" placeholder="Hint: The date of your birthday!!!">
  <br>
  <button onclick="unlock()">Enter 💖</button>
  <p id="error"></p>
</div>

<!-- MAIN VALENTINE PAGE -->
<div id="valentine" style="display:none;">
  <h1>Will you be my Valentine, Dione? 💕</h1>

  <!-- IMAGE (hidden at first) -->
  <div id="valentine" style="display:none;">
  <h1>Will you be my Valentine, Dione? 💕</h1>

  <!-- IMAGE (hidden at first) -->
  <img id="yesImage" src="love.png"
       style="display:none; width:300px; border-radius:20px; margin:20px auto;">

  <br>
  <button onclick="yes()">YES 💖</button>
  <button id="no" onmouseover="move()">NO 😢</button>
</div>

<!-- BACKGROUND MUSIC -->
<audio id="music" autoplay loop>
  <source src="music.mp3" type="audio/mpeg">
</audio>

<script>
  // PASSWORD SCREEN
  function unlock() {
    const correct = "131008"; // <-- change to your secret code
    const input = document.getElementById("pass").value;
    if(input === correct){
      document.getElementById("lock").style.display="none";
      document.getElementById("valentine").style.display="block";
    } else {
      document.getElementById("error").innerText="Hmm… that’s not it 😏";
    }
  }

  // YES button
  function yes() {
    document.getElementById("videoMusic").style.display="block"; // start music
    document.body.innerHTML = "<h1>YAY!!! 🥰💘<br>Dione, you’re my Valentine forever!</h1>";
    createHearts();
  }

  // NO button chaos
  function move() {
    const no = document.getElementById("no");
    no.style.left = Math.random() * (window.innerWidth - 100) + "px";
    no.style.top = Math.random() * (window.innerHeight - 100) + "px";
  }

 // Floating hearts
function createHeart() {
  const heart = document.createElement("div");
  heart.className = "heart";
  heart.innerText = "💖";
  heart.style.left = Math.random() * window.innerWidth + "px";
  heart.style.top = window.innerHeight + "px";

  document.body.appendChild(heart);

  setTimeout(() => {
    heart.remove();
  }, 4000);
}

// More hearts 💕
setInterval(createHeart, 150);
.heart {
  position: absolute;
  color: red;
  font-size: 24px;
  pointer-events: none;
  animation: floatUp 4s ease-in-out forwards;
}

@keyframes floatUp {
  0% {
    transform: translateY(0) scale(1);
    opacity: 1;
  }
  100% {
    transform: translateY(-700px) scale(1.3);
    opacity: 0;
  }
}

  function animateHeart(heart){
    let top = window.innerHeight;
    const id = setInterval(()=>{
      top -= 2;
      heart.style.top = top + "px";
      if(top < -50) {
        heart.remove();
        clearInterval(id);
      }
    },20);
  }
</script>

</body>
</html>
